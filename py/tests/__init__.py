"""This repo's test suite. Run it through py/main_test.py.

The package marker is what lets unittest discovery import these modules as
``tests.test_<x>`` under ``py/``, which is the directory py/main_test.py puts
on sys.path by being where it is. Nothing here configures a path.
"""
